# Kinnectfi — Frontend (FE)

Reserved for the React Native (Expo) mobile app. Empty for now; the app will be initialized in a dedicated development session.

- Stack (locked): React Native + Expo, Privy `@privy-io/expo` (auth + embedded wallet), Sumsub WebView (KYC), Transfi hosted UI via `react-native-webview`.
- Backend lives in the sibling `../BE/` directory.
- See `/Users/mab/Desktop/claude/projects/kinnectfi/` for HLD + LLD docs + decisions log.
